# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Fiesta' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Fiesta
    pod "SwiftyJSON"
  # pod "GoogleMap"
  # pod "GooglePlaces"
    pod "Alamofire"
    pod "Firebase/Analytics"
    pod "Firebase/Messaging"

  target 'FiestaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FiestaUITests' do
    # Pods for testing
  end

end
